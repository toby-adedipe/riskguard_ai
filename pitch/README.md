# Archived hackathon pitch

The `AI4Telco_*` files, generated slide images, and `build.js` preserve the
original hackathon deck. They describe the retired deterministic Semantic
Kernel demo and are not evidence of current `dev` capabilities.

Do not use these artifacts for a current customer conversation without
rewriting and regenerating them from the active runtime status and the
north-star layer specifications in `../docs/`.

`mtn-meeting/04-product-roadmap.md` is different: it is retained as product
strategy and is linked from the north-star agent plan.
